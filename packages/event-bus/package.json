{
  "name": "@khelo/event-bus",
  "version": "0.1.0",
  "private": true,
  "description": "Abstract event bus with in-memory implementation, designed for Kafka migration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "lint": "eslint src/ --ext .ts",
    "lint:fix": "eslint src/ --ext .ts --fix",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@khelo/types": "*",
    "@khelo/logger": "*"
  },
  "devDependencies": {
    "@khelo/config": "*",
    "@typescript-eslint/eslint-plugin": "^8.20.0",
    "@typescript-eslint/parser": "^8.20.0",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^9.1.0",
    "typescript": "^5.7.3"
  }
}
